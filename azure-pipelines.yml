variables:
 - group: vargroup
 - name: connstr
   value: test

parameters:
  - name: environment
    displayName: Which Environment to deploy?
    type: string
    default: 'develop'
    values:
    - develop
    - preproduction
    - production

steps:
  - script:
     echo "Hello $(parameters.environment)"
     echo "variable group $(var1)"
    displayName: step1

